# Tooling

This folder houses optional helper scripts (Python, CLI utilities, data pipelines). Drop new utilities here and gate their dependencies through `requirements-dev.txt`.

Suggested subfolders:
- `tools/batching/` → asset importers, sprite slicing.
- `tools/balance/` → spreadsheets → JSON exporters for tuning.
- `tools/telemetry/` → log inspection utilities.
